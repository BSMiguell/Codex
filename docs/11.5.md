# §11.5 — Camadas extras do mapa

**Status:** Iniciado (base/documentação)  
**Referência:** `Temporario.md` (Camada 6 → próximo passo), `docs/checklist-validado.md` (#16)  
**Dependência:** `§11.9` (worldbuilding: magia com regras e custos)

## Camadas propostas

| Nome | Tipo | Fonte de dados | Status |
|---|---|---|---|
| `política` | Facções/reinos/impérios | `historia-api.json` — `battles[].lados` (raças envolvidas) | 🟡 Aguardando conteúdo |
| `mágica` | Sistema de magia | `§11.9` (regras, custos, rituais) | 🟡 Aguardando conteúdo |
| `rotas` | Caminhos entre regiões | `historia-api.json` — `regions[]` | 🟡 Aguardando conteúdo |
| `conflitos` | Detalhes das batalhas | `historia-api.json` — `battles[]` | 🟡 Aguardando conteúdo |

## Implementação técnica

O `Mapa_Aetheria.html` usa o sistema genérico `CAMADAS` (`data-camada` + boolean). Adicionar novas camadas é trivial:

```js
CAMADAS.política  = { label: 'Política', color: '#5a4a3a', icon: '🏰', active: false };
CAMADAS.mágica = { label: 'Magia', color: '#6a3d9a', icon: '✨', active: false };
```

Não requer alteração estrutural no HTML ou JS — apenas conteúdo (`worldbuilding`) e definição dos objetos.

## Próximo passo

Definir `§11.9` (sistema de magia) para desbloquear o conteúdo das camadas `mágica` e, indiretamente, `política` (facções dependem de regras de magia/religião no lore).
