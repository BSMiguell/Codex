# Memória diária — 09/09/2026

## Objetivo

Atualizar o novo sistema de memória diária do Aetheria Codex e registrar o fechamento da Camada 2 do plano operacional.

## Trabalho realizado

- A **Camada 2 — URL e SEO** foi concluída e validada.
- As URLs públicas foram migradas de `https://bsmiguell.github.io/Temporario` para `https://bsmiguell.github.io/Codex` nos arquivos aplicáveis.
- A home foi corrigida para refletir a fonte de verdade atual de **487 personagens**, eliminando referências públicas a 489.
- Canonical, Open Graph, Twitter Card, sitemap, páginas de raça e referências relacionadas foram alinhados à produção em `/Codex`.
- `tests/url-check.mjs` foi ajustado para ignorar **somente** `scripts/migrate_codex_urls.ps1`, pois esse script precisa conservar a URL antiga como origem da migração.
- O script de migração havia removido o BOM UTF-8 de arquivos que já possuíam BOM; os BOMs originais foram restaurados antes do commit.
- A validação final confirmou ausência de alterações artificiais de BOM e somente as mudanças intencionais de URL/SEO.

## Validação

`node tests/url-check.mjs`:

```text
OK — URLs de produção em /Codex; 165 arquivos textuais verificados; SEO da home = 487 personagens.
```

`git diff --check` não encontrou erros de whitespace; os avisos LF/CRLF foram apenas os avisos normais do Git no Windows.

## Git

Commit da Camada 2:

```text
06c4e05 — fix: migra URLs e SEO para produção
```

O commit contém 29 arquivos alterados.

Push concluído:

```text
426dfde..06c4e05  main -> main
```

Não foi utilizado force push.

## Decisões e lições

- O script `scripts/migrate_codex_urls.ps1` deve continuar contendo a URL antiga `/Temporario`, porque ele é justamente o mecanismo de migração.
- O teste de URLs deve tratar esse arquivo como exceção explícita, sem abrir exceções para outros arquivos.
- Migrações textuais precisam preservar o BOM original quando os arquivos já o utilizam.
- A fonte de verdade do número de personagens continua sendo **487**.

## Estado do plano

```text
Camada 0  🟢
Camada 1  ✅ CONCLUÍDA
Camada 2  ✅ CONCLUÍDA
Camada 3  🟡 EM ANDAMENTO — VERSION atualizada (v1.4.0), precache expandido, análise concluída
```

## §9.2 — Coleções temáticas (registrado em 09/09/2026)

Status: ✅ CONCLUÍDO. A vitrine de raças (`.hero-races`) com carrossel infinito de 22 coleções já está no `index.html` (linha 461), CSS (`assets/codex.css` §9.2 v2) e JS (`renderRaces()`). Todos os 22 links `racas/*.html` funcionam. Checklist #36 (`docs/checklist-validado.md`) confirma. Nenhum código pendente; registro feito no `Temporario.md` (scratch).

Iniciar a **Camada 3 — Cache / PWA / Offline**, começando por uma análise de `sw.js` antes de qualquer alteração.

Pontos a verificar:

1. versão atual do Service Worker;
2. estratégia real de cache;
3. arquivos em precache;
4. invalidação de cache;
5. compatibilidade com os 487 personagens;
6. diferença entre o comentário de stale-while-revalidate e a implementação real;
7. relação entre `MAX_RUNTIME = 200` e o objetivo de funcionamento offline.

Nenhuma alteração da Camada 3 deve ser feita antes dessa análise.

---

## §7.1 — Páginas de raça únicas (continuação, 09/09/2026)

`build_racas.ps1`: `$dedicatedFolders` expandido (2 → 9 raças). `node tests/validate-api.mjs` passa. `npm run smoke` ainda não re-rodado (não necessário — a mudança é puramente no template HTML, zero impacto no site de galeria). Próxima sessão: regenerar screenshots das 9 páginas dedicadas (se necessário) ou seguir para §9.2.
